{
  "name": "orirando-e2e",
  "private": true,
  "description": "Browser smoke tests against the compose dev stack. Own package so the site bundle's lockfile never churns for a test dependency.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
